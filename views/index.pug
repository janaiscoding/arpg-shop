extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/items') Total items #{item_count}<br>
  a(href='/categories') Total categories  #{category_count}

  h1 Create new content 
  a(href='/category/create') Create new category