extends layout

block content
  h1= title
    h1 #{item.name}
    p #{item.description}
    a(href=item.category.url) #{item.category.name}
    p #{item.price} Divines 
    p #{item.stock} in Stock  
