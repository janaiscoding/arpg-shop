extends layout

block content 
    h1 Do you really want to delete this item: #{item.name}?

    form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=item._id )

      button.btn.btn-primary(type='submit') Delete