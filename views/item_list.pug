extends layout

block content
  h1= title

  ul
  each val in list_items
    li
      a(href=val.url) #{val.name} 
      p #{val.description}
      p #{val.price} Divines 
      p #{val.stock} in Stock
  else
    li There are no items.